import React from 'react';
const FilterContext = React.createContext()

export const FilterProvider = RestaurantContext.Provider
export const FilterConsumer = RestaurantContext.Consumer

export default FilterContext